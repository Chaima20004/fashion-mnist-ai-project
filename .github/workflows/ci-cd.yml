name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
    
    - name: Set up Python 3.9
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Lint with flake8
      run: |
        pip install flake8
        flake8 src/ --count --select=E9,F63,F7,F82 --show-source --statistics
        flake8 src/ --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    
    - name: Format check with black
      run: |
        python -m pip install black
        black --check src/ || echo "Note: Code formatting can be improved (not blocking)"
      continue-on-error: true
    
    - name: Run unit tests
      run: |
        if [ -d "tests" ]; then
          python -m pytest tests/ -v
        else
          echo "No tests directory found, skipping unit tests"
        fi
    
    - name: Test imports
      run: |
        python -c "
        try:
            import torch
            import torchvision
            import numpy as np
            import pandas as pd
            import matplotlib.pyplot as plt
            import yaml
            print('All imports successful')
        except ImportError as e:
            print(f'Import error: {e}')
            exit(1)
        "
    
    - name: Validate config files
      run: |
        python -c "
        import yaml
        try:
            with open('train_config.yaml', 'r') as f:
                config = yaml.safe_load(f)
            print(f'train_config.yaml loaded successfully')
            print(f'Config structure: {list(config.keys())}')
        except Exception as e:
            print(f'Error in train_config.yaml: {e}')
            exit(1)
        "
        
        if [ -f "requirements.txt" ]; then
          echo "requirements.txt found"
        else
          echo "requirements.txt not found"
          exit 1
        fi

  docker-build:
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/main'
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2
    
    - name: Build Docker image
      run: |
        docker build -t fashion-mnist-train:latest -f Dockerfile.train .
        echo "Training Docker image built successfully"
        
        if [ -f "Dockerfile.app" ]; then
          docker build -t fashion-mnist-app:latest -f Dockerfile.app .
          echo "App Docker image built successfully"
        else
          echo "No app Dockerfile found, skipping"
        fi
    
    - name: Log Docker images
      run: |
        docker images | grep fashion-mnist
